# Absensi Backend

Backend service for Absensi application built with Go and Gin.

## Setup

1. Copy `.env.example` to `.env`
2. Run `go mod tidy`
3. Run `go run ./cmd/api/main.go`

## Docker

```bash
docker-compose up --build
```
